  <h2>ユーザー</h2>
  <% @users.each do |user| %>
        <p><%= user.email %></p>
        <p><%= user.name %></p>
      </div>
<% if user != current_user %>
  <%= render 'follow_form', user: user %>
<% end %>
  <% end %>
  <%= link_to '戻る', topics_path %>
