  <h2>ユーザー</h2>
  <% @users.each do |user| %>
    <% if user.confirmed_at && user != current_user %>
        <%= profile_img(user, "40x40") if profile_img(user, "40x40") %><br>
          <p><%= user.name %></p>
          <% if user.following?(current_user) %>
            <p>あなたをフォローしています</p>
          <% end %>
         </div>
       <%= render 'follow_form', user: user %>
   <% end %>
 <br>
  <% end %>
  <%= link_to '戻る', topics_path %>
