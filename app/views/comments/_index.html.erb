<ul>
  <% comments.each do |comment| %>
    <% unless comment.id.nil? %>
      <li>
        <%= profile_img(comment.user, "40x40") if profile_img(comment.user, "40x40") %>
        <p class="left"><%= comment.user.name %>さんがコメントしました。</p>
        <p class="left"><%= comment.content %></p>
        <% if current_user.id == comment.user.id %>
            <%= link_to '編集', '#' %>
            <%= link_to '削除', topic_comment_path(topic, comment), method: :delete, remote: true, data: { confirm: '本当に削除していいですか？' } %>
          </p>
        <% end %>
      </li>
    <% end %>
  <% end %>
</ul>
